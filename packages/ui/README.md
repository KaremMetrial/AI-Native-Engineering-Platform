# UI

Shared React component library used across `apps/web` features (`03`,
`docs/delivery/11-repository-and-folder-strategy.md`).

**Empty until a component is genuinely shared by more than one feature.**
Per naming standards D-577's sibling principle for prompts (applied here to
components): extract on the second real occurrence, not speculatively.
