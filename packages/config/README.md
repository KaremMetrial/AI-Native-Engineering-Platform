# Config

Shared tooling configuration used across more than one app -- e.g. a common
Prettier or TypeScript base config once `apps/web` is joined by another
TypeScript app that should share it.

**Empty for now.** With only one TypeScript app in the monorepo, there is
nothing to share yet; each app's own config is complete and self-contained.
