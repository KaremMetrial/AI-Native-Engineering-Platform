# Lib

Framework-adjacent integrations: the realtime gateway client, telemetry setup,
third-party SDK wrappers. Code here is aware of the outside world (browser
APIs, WebSocket, analytics) in a way `shared/` deliberately is not.
