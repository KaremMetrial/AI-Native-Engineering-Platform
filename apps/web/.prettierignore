# schema.ts is generated by openapi-typescript from packages/contracts
# (D-110) -- regenerating it always overwrites formatting, so there is no
# reason to enforce or maintain it here.
dist
coverage
src/lib/api/schema.ts
